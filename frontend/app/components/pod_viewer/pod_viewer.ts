import {Component} from '@angular/core';

/** A pod viewer component. */
@Component({
  selector: 'pod-viewer',
  templateUrl: './pod_viewer.ng.html',
  styleUrls: ['./pod_viewer.css']
})
export class PodViewer {
  title = 'Pod Viewer WIP';
}
