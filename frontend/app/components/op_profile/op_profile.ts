import {Component} from '@angular/core';

/** An op profile component. */
@Component({
  selector: 'op-profile',
  templateUrl: './op_profile.ng.html',
  styleUrls: ['./op_profile.css']
})
export class OpProfile {
  title = 'Op Profile WIP';
}
