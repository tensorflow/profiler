import {ActionCreator, MemoizedSelector} from '@ngrx/store';

/** Action type. */
// tslint:disable-next-line:no-any
export type ActionCreatorAny = ActionCreator<any, any>;

/** Selector type. */
// tslint:disable-next-line:no-any
export type MemoizedSelectorAny = MemoizedSelector<any, any>;
