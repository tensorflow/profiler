/** @enum {string} */
export const enum IdleOption {
  YES = 'Yes',
  NO = 'No',
}

/** @enum {string} */
export const enum OpExecutor {
  NONE = '',
  DEVICE = 'Device',
  HOST = 'Host'
}

/** @enum {string} */
export const enum OpKind {
  NONE = '',
  NAME = 'Name',
  TYPE = 'Type'
}
